/*
This example creates text in a new sheet if a spreadsheet is available.
*/

LibreOffice.newSheet("Info");
for (int i = 0; i < 20; i++) {
	for (int u = 0; u < 20; u++) { 
		int x=(int)(Math.random()*50);
		int y=(int)(Math.random()*50);
		LibreOffice.insertText("Info",x,y);
        }
 }
